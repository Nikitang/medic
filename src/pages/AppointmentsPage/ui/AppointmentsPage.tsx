import { Appointments } from 'features/Appointments';

const AppointmentsPage = () => {
    return (
        <div>
            <Appointments />
        </div>
    );
};

export default AppointmentsPage;
